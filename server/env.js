import "dotenv/config";

const {
  PORT,
  DB_URL,
  SMTP_ADDRESS,
  SMTP_PASSWORD,
  OAUTH_CID,
  OAUTH_SECRET,
  OAUTH_REFRESH,
  OAUTH_ACCESS,
} = process.env;

export {
  PORT,
  DB_URL,
  SMTP_ADDRESS,
  SMTP_PASSWORD,
  OAUTH_CID,
  OAUTH_SECRET,
  OAUTH_REFRESH,
  OAUTH_ACCESS,
};
